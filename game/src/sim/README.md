# src/sim — シミュレーション中核

`ui` → `sim` → `core` の一方向依存。sim は DOM も window も `Date.now()` も触らない。
乱数は必ず引数で渡された `core/rng.js` の `RNG` から引く（`Math.random()` は1箇所もない）。

## ファイル

| ファイル | 中身 |
|---|---|
| `index.js` | 公開API。ui と test はここだけを import する |
| `constants.js` | チューニング定数。バランス調整はここだけで済むようにしてある |
| `genetics.js` | 33座位／8染色体。優劣・中間遺伝・交叉・腕予算・遺伝的荷重 |
| `derive.js` | 導出層。実効値／七つの大罪／国民力／戦闘係数／産出・消費 |
| `world.js` | createWorld / stepTick / advanceGeneration / 置く / 読む |
| `chronicle.js` | 年代記。事件レコード・出所・遡行・保持 |
| `cards.js` | 方針カード11枚と、局長の人格による歪み |
| `petitions.js` | 具申の生成と裁定 |
| `battle.js` | 個体単位の戦闘・降伏・捕虜・国境処理・ゴースト |
| `rival.js` | 10の経営思想プロファイルと自動オーナー |
| `roster.js` | 10国を並行に走らせるロスター |

## 実装したもの

### 遺伝

- **心系＝離散の優劣**。対立遺伝子2つを持ち、優性が1つでもあれば劣性は隠れる。
  劣性ホモになった瞬間に潜伏していた値が表に出る。創世時の劣性は極端な値を引きやすくしてあるので、
  数世代潜伏したあと突然発現する挙動が出る（テストで実測できている）。
- **体系＝中間遺伝**。2本の平均＋ゆらぎ。
- **可塑（ch0）が減数分裂の交叉率そのもの**。低い血統は親のコピーに近く、カーストが固まる。
- **対抗アームの予算**。同じ染色体の A/B は `mean(A) + mean(B) = 1.0` に厳密正規化される。
  上限（超えたときだけ縮める）ではなく等式にしてある。上限だと世代を重ねるごとに
  下方ドリフトして1000世代で全員が虚弱になるため。
  **対立遺伝子と表現型の両方に掛ける**——心系はドミナンスで非線形なので、
  対立遺伝子だけ正規化しても表現型では予算を超えられてしまう（実測で染色体単位の違反が出た）。
- **遺伝的荷重**。劣性は「潜伏している値」だけでなく「潜伏している欠陥」も運ぶ（`allele.load`）。
  劣性ホモの座位でだけ発現し、産出・繁殖・戦闘・寿命の全部に掛かる `vitality` になる。
  これがないと劣性ホモ率が上がっても腐りはしないので、近親交配ペナルティも雑種強勢も成立しない。

### 素質・発現・練度

```
実効値 ＝ 素質(遺伝) × 発現(幼少期にその局面に置かれたか) × 練度(環境係数 × 実活動)
```

- **練度は一切遺伝しない。** 子は必ず全練度0で生まれる（テストで親子相関 r=0 を確認）。
- 伸び率は環境が決める：生育地（辺境／中心）・家業（親の役）・貧富（1人あたり食料）。
- 発現は幼少期（age ≤ 1）の2回だけ判定する。逃すと一生開かない。
  辺境で育つと戦の側が勝手に開き、中心部は開きにくい。
- **未発現の素質は `readGene()` がレンジでしか返さない**（真値±10%）。

### 欲求

- 七つの大罪 ＝ 既存遺伝子 × 感受性(8番)。すべてに感受性が掛かる。
- **すべての罪にプラスの出力がある**（怠惰＝長命・疲労耐性、暴食＝飢餓耐性、憤怒＝戦闘力、
  傲慢＝統率、強欲＝産出、嫉妬＝練度、色欲＝繁殖）。潰すと必ず何かを失う。
- 内発／外発は幼少期にその欲が「行為」で満たされたか「報酬」で満たされたかで決まる。
  内発は報酬ゼロでも動く（`unmetBySin` で未充足度が落ちる）。

### 方針カード12枚（全部が配線されている）

| カード | 局 | 何に効くか |
|---|---|---|
| `deploy_top` | 軍務 | 派遣する上位N% |
| `spare_old` | 軍務 | N歳以上を温存 |
| `raise_young` | 軍務 | 育成派遣の比率 |
| `guards` | 軍務 | 護衛の随伴数（運死が下がる／その体は他の役に就けない） |
| `drill` | 軍務 | 模擬戦に割く比率（P2から） |
| `surrender_at` | 軍務 | 団結がN%を切ったら降伏具申。**開戦時に確定**する |
| `hunt_ratio` | 農業 | 狩りに割く比率 |
| `stockpile` | 農業 | 備蓄をN日分確保。割ると配給を絞る＝食い延ばせるが不満が溜まる |
| `frontier` | 民生 | N%を辺境へ。環境係数＝練度の伸びに直結する育成レバーの本体 |
| `ration_equal` | 民生 | 配給の平等さ。傾ければ産出が上がり、弱い者から飢え、下位層に怨恨 |
| `hereditary` | 民生 | 世襲の尊重＝**透過率**。低いと子が親の役を継ぎ、高いと適性で配る |
| `mix_policy` | 民生 | 融和か隔離か |

**AIも同じカードを通る。** `frontier` / `hereditary` / `mix_policy` は
ライバル国のプロファイルからカードに焼き込んでいる。AIだけが別経路を持つと、
「相手のほうがレバーが多い」という不公平が静かに戻ってくるため。

実測（45世代 × 5種、カードを振ったときの差）：

| | 人口 | 備蓄 | 産出 | 民心 | 怨恨 | 辺境比 |
|---|---|---|---|---|---|---|
| 既定 | 130 | 103 | 78.1 | 0.55 | 0.00 | 0% |
| `stockpile=50` | 253 | 514 | 96.2 | 0.32 | 0.16 | 0% |
| `frontier=90` | 169 | 50 | 108.9 | 0.48 | 0.00 | 91% |
| `ration_equal=0`（傾斜） | 76 | 4 | 47.6 | 0.33 | 0.24 | 0% |
| `hereditary=0`（実力主義） | 130 | 62 | **87.4** | 0.52 | 0.00 | 0% |
| `hereditary=100`（世襲） | 143 | 82 | **73.0** | 0.46 | 0.08 | 0% |

`surrender_at` は「早く降りれば安く、粘るほど高くつく」が実測で出る
（閾値90%で代価20.1／30%で27.4。低い閾値ほど崩壊が先に来て降伏の機会自体が減る）。

### 融和か隔離か（`mix_policy` カード）— 応答はU字である

最適解探索で最適値が 1.00 と 0.00 の間で反転した。原因は「効いていない」ではなく
**応答が単調でない**こと。線形の当てはめは、標本がどちらの端に寄るかで係数の符号が
入れ替わる。

200世代 × 16種、同じ種での対応比較：

| | 時間平均人口 | 勝率 | 混血度 |
|---|---|---|---|
| 0%（隔離） | 102.8 | 31% | 0.034 |
| 50%（中途半端） | **88.5** | 32% | 0.401 |
| 100%（融和） | 106.0 | 38% | 0.741 |

- 100% が 50% を上回った世界 **13/16**（平均差 +17.5人）
- 0% が 50% を上回った世界 7/16（平均差 +14.3人。外れ値が効いており弱い）
- 100% が 0% を上回った世界 11/16（平均差 +3.2人）

**谷は真ん中にある。** 融和が最良、隔離は分散が大きいが有効、
半端に混ぜるのがいちばん悪い。隔離が成立するのは、斑のまま固定すると
2つの遺伝子プールが別々に保たれて、どちらも小集団として腐りきらないため。

統治思想の第一問として、これは望ましい形（どちらかに振り切る意味がある）。
探索側は単調モデルではなく、少なくとも2次以上か端点比較で当てること。



フェーズ2の第一問。0＝隔離 … 100＝融和で `world.mating.foreignBias` を動かす。
実測（60世代・3種平均）：

| 融和度 | 外来血 | 混血度 | 純血個体 | 設計文書の表 |
|---|---|---|---|---|
| 0%（隔離） | 38.1% | 0.006 | 96% | **斑のまま固定** |
| 50%（中立） | 9.2% | 0.083 | 37% | 斑 |
| 100%（融和） | 50.3% | 0.488 | 1% | **混色** |

**外来血の「量」と「混ざり具合」は別物**である点に注意。隔離政策では
よそ者が自分たちだけで繁殖して系統が薄まらないので、量はむしろ増えたまま
混血度だけがゼロに張り付く。同化のメーターは量ではなく `stats.admixture`
（1 − 血統の最大成分）のほう。色が混ざる＝遺伝が混ざる、が同じ処理になっている。

### 怨恨（2種を分けてある）

| | 宛先 | 消えるか | 実装 |
|---|---|---|---|
| 体制怨恨 | 民衆 → 国 | 消えない。家系に継承 | `ind.regimeGrudge` ＋ `ledger` の出所ごと継承 |
| 個人怨恨 | 個体 → 個体 | 本人の死で消える | `ind.grudges[targetId]`、世代境界で死者分を削除 |

### 戦闘

- 個体単位。集計値の突き合わせはしていない。
- 勝敗は殲滅ではなく**団結の崩壊**。逃走が増えるほど団結が落ち、敗走カスケードになる。
- **逃走癖の頻度依存**：少数なら生き延び、多数だと敗走して逃げた者から死ぬ
  （`ROUT_FLED_DEATH + ROUT_FLED_CROWD × 逃走率`）。逃げた個体は戦功が付かず、
  繁殖機会も落ちる（`chooseMate` の `cowardice` ペナルティ）。
- 戦死は**ステータス由来90% / 完全ランダム10%**。決着後に総戦死数から逆算して
  流れ矢を引く（ラウンド中のHP撃破にだけ運死を付けると、敗走時の追い討ちが母数に入って
  実測2%にしかならなかった）。個体に `deathByLuck` / `deathDetail` が残る。
- 降伏はオーナー裁定。代価は戦況の関数（早く折れば安く、崩壊後は不可）。
  資源が動くのは降伏のときだけ。

### 年代記

- 事件レコードは `trueCause`（真の原因）と `claimed`（公表された帰属）を別カラムで持つ。
- 追跡対象5本（怨恨/民心/産出率/血統/帰属）の増分に原因イベントIDが付き、
  出生時に子へ引き継がれる。だから3世代後の一揆から原因の粛清まで `traceUp` で遡れる。
- 保持は到達可能性。生きている怨恨から辿れる事件は無名の農民の冤罪でも残す。

### 具申

- LLMは使わない。局長の人格ベクトル × 世界状態で11種のテンプレを重み付けし、
  **誰が × 誰を相手に × 何を賭けて**（予算/人材/名誉/命/方針）の積で見え方を散らす。
- 承認しても却下しても必ず誰かに怨恨が積む。無害な選択はない。

### オープニング：回答は種族の重心

`createWorld(seed, spec, opts)` が受ける `spec` は3つの形を取る（後方互換）。

```js
createWorld(seed, { centroid: { 誇り: 0.72, 野心: 0.31, … }, spread: 0.16 })  // 本格診断
createWorld(seed, [0.8, 0.2, …])                                            // 旧・回答の配列
createWorld(seed, {} | null)                                                // 平凡な種族
```

- `centroid` は**心系22座位のみ**。体系（代謝・頑健・攻撃素質…）は別枠で振る。
  `spread` にも連動させていない（`BODY_FOUND_SPREAD` 固定）——診断の解像度を
  上げたときに体格まで揃ってしまわないようにするため
- アダムとイザナミは重心から**独立にサンプリング**される。作るのは個体ではなく種族
- 劣性対立遺伝子は重心と無関係に極端値から引かれるので、**潜伏する劣性は最初から仕込まれている**
- `world.spec.centroid` に**実際に成立した重心**が入る。要求どおりとは限らない（下記）

**要求した重心がそのまま通るとは限らない。** 対抗アームの予算
（mean(A)+mean(B)=1.0）は構造的な制約なので、「誇りも序列意識も高い種族」
（5番の両アーム）は要求しても作れない。実測で 誇り0.9 の要求が 0.75 に落ちる。
`projectCentroid(centroid)` が成立する分を返すので、**診断結果をそのまま表示せず
こちらを見せること**（さもないと「答えたとおりになっていない」と読める）。

### 創世の spread ＝ 0.16（実測で決めた）

4種族 × 8種 × 20世代で測った。

| spread | 回答の残存 r | 種族の分離 | 潜伏形質の発現 | 集団のばらつき | 二匹の距離 |
|---|---|---|---|---|---|
| 0.12 | 0.781 | 2.16 | 127 | 0.0137 | 0.89 |
| **0.16** | **0.743** | **1.88** | **129** | **0.0159** | **1.02** |
| 0.18 | 0.727 | 1.82 | 125 | 0.0164 | 1.09 |
| 0.20 | 0.678 | 1.58 | 125 | 0.0179 | 1.14 |

読み方：

- **「小さいと交叉が回らない」は、この遺伝システムには当てはまらなかった。**
  潜伏形質の発現はどの spread でもほぼ一定（125〜130件）。劣性対立遺伝子は
  重心と無関係に極端値から引かれるので、二匹の優性が似ていても交叉は回る。
  7問のときに回らなかったのは spread ではなく、**7つの回答を22座位に循環で
  割り当てていた**ことが原因。重心を22座位ぶん受け取る時点でその問題は消える。
- したがって効いているのは「回答の残存」と「集団のばらつき」の綱引きだけ。
  残存は spread に対して単調に落ちる。**0.20 で分離が 1.82 → 1.58 と折れる**ので、
  その手前で、かつ標準変異をできるだけ確保できる 0.16 を採った。
- 強い選択（統治）を掛けた世界では、20世代で残存 r が 0.74 → 0.29 まで落ちる。
  これは壊れているのではなく設計どおり——「国が完成に近づくほど初期ステータスは
  誤差になり、それまでの経営手腕が結果を支配する」。回答が効くのは序盤の窓だけ。

### 序盤（P1）の順番

設計の要求どおり **10体到達 → 初戦 → 戦後処理 → フェーズ2** の順で進む。

- 人口が10に達すると `world.pendingFirstWar` が立ち、フェーズはまだ村のまま足踏みする。
  `settleWar` が `firstWarDone` を立てて初めてフェーズ2に上がる。
  人口だけで上げていたときは初戦より先に部族フェーズへ入り、
  UIでは配役タブが消えたあとに初戦が来ていた。フェーズ2は外来血の流入で幕を開ける。
- 初戦だけは派遣カードも相手の規模も通さず **5対5** に固定する。
  成人が4人しかいなければ年長の子どもで埋める（＝育成派遣。恐怖耐性と統率は
  実戦でしか本命が伸びないので、初戦は育成の機会でもある）。
- フェーズ1のゴーストは「隣の村」であって国ではない。人数・練度・発現率を村と同格にする。
- 捕虜の人数は **開戦時のフェーズ**（`battle.phase`）で決まる。フェーズ1は1体。
  `captiveOptions().count` と `takeCaptives` は同じ `captiveRange()` から出すので食い違わない。
- **その1体が生き延びて子を残せること**が、フェーズ2の存在理由そのもの
  （「10体の村に1体＝色が変わる」）。ここは3つの噛み合わせで壊れていた：

  | | 元 | 現在 |
  |---|---|---|
  | 余命1世代未満で入国する率 | 17% | **3%** |
  | 子を残せた率 | 77% | **87%** |
  | 12世代後の外来血 | 8.6% | **14.3%**（入国時10%より増える） |
  | 外来の血が絶えた世界 | 30% | **17%** |

  残る17%は単一個体の系統がドリフトで絶える分。捕虜が土着のいとこ・氏族より
  不利なのは残しているが、それを覆すのが `mix_policy`（融和）カードであり、
  「融和か隔離か」という統治の第一問そのものなので、既定で埋めてはいない。

### 10国ロスター

プレイヤーの世界とまったく同じ `createWorld` / `advanceGeneration` で走る。
特別扱いは一切していない（同じ遺伝・同じ戦闘・同じ怨恨・同じ具申）。
自動オーナーは8つの動詞のうち **置く・裁く・殺す・敷く** を撃つ。

思想の差が血統の差になる経路は3本だけで、どれもラマルクではなくダーウィン：
1. **繁殖機会** — 地位・実績・オーナーの選好（`mating.prefer`）が交配相手の重みになる
2. **粛清** — 閾値を超えた形質の個体が消える
3. **生存** — 望まれた形質は中心部と良い配給に置かれ、望まれない形質は辺境に送られる

## 実測（`node test/run.js`）

142世界・200世代。AAA 6項目すべて PASS、終了コード0。

| | 結果 |
|---|---|
| 決定性（同じ種＝同じ歴史） | PASS |
| 不変条件（NaN・人口爆発・無限ループ） | PASS |
| 即死しない | PASS（10世代以内の絶滅 0/142） |
| 連鎖（全ステ／染色体単位で親2人を上回る子） | PASS（0件） |
| 頻度依存（逃走・私欲・怠惰が固定しない） | PASS |
| 劣性の潜伏 | PASS |
| 練度が遺伝しないこと | PASS |
| 戦死の内訳 90/10 | PASS |
| 年代記の遡行 | PASS |
| purist vs melting（腐る／雑種強勢） | PASS |
| P1: 初戦とフェーズ2の順序 / 5対5 / 捕虜1体 / 3〜4世代 | PASS |
| 近親交配（閉じると腐る／外来血で戻る） | PASS |
| オーナーごとの分化 | PASS |
| merit vs dynastic（透過率が出自分布を変える） | PASS |
| martial vs agrarian（淘汰で武力分布がずれる） | PASS |

## 「捕虜が薬になる」が逆転していた件（実測と原因）

最適解探索で国境処理の回帰係数が `return +267 / accept −19 / kill −248` となり、
上位8方針すべてが送還を選んだ。設計の第2の柱（捕虜＝薬、戦争の恒久的な経済理由）の
逆である。原因は**設計の誤りではなく実装の穴**だった。

**移民が自国民より遺伝的荷重を2.18倍持っていた。**
自国民は60世代の純化淘汰を経ているのに対し、ゴーストは創始者と同じ新規生成で
無淘汰だったため。雑種強勢（ヘテロ化による荷重の隠蔽）は正しく効いていたが、
同時に**実在の国なら持っていない未淘汰の欠陥を輸入**していて、そちらが勝っていた。

年齢と寿命の不整合を直したときと同じ種類のバグ——**ゴースト生成器だけが、
実在の集団が満たす不変条件を満たしていなかった**。

直し方（2つとも構造の修正で、係数合わせではない）：
- 走っている国は突然変異と淘汰が釣り合った水準にいるので、創世とは別の
  荷重率を使う（`GHOST_LOAD_P`）。創世は「無から2体を作る」、ゴーストは
  「何世代も走っている国から個体を引く」で前提が違う
- 候補を多めに作り保有荷重の軽い順に残す（`GHOST_OVERDRAW`）。
  戦場で会う相手は、その国で成人まで生き延びた者である

修正後（200世代 × 24種、時間平均）：

| 国境処理 | 人口 | 保有荷重 | 劣性ホモ | 生存世界 |
|---|---|---|---|---|
| return | 85.3 ± 49.7 | 2.24 | 0.131 | 23/24 |
| **accept** | **86.3 ± 45.4** | **2.06** | **0.108** | **24/24** |
| kill | 80.9 ± 46.5 | 2.17 | 0.138 | 23/24 |

受け入れが**荷重と劣性ホモの両方で最良**になり、生存も最良になった。
ただし人口・産出の差は分散の中で、**「戦わざるを得ない」ほどの強制力は無い**。
設計の主張は符号としては成立するが、強度は設計文書の言い回しより弱い。

## v3 に持ち越したもの（記録）

- **`setCanon()` は正史を書くだけで、露見判定を持たない。** 設計文書の
  「嘘は安いが賭け（露見リスク）／真実は高いが確定」が、現状はどちらも同じ挙動になる。
  露見確率の入力（諜報・祭祀局の性能・扇動者の存在・国民の知性）が
  どれもv2の範囲外なので、「編む」がP3の動詞であることと合わせて保留にしてある。

## 実装しなかったもの

SPEC の「v2に入れないもの」に従い、以下は枠だけか未着手：

- 実PvP／国交・亡命／P3・P4／疫病・災害／保因者検査／諜報局・教育局
- 戦争・生産以外の8局面（技術・経済・内政・繁殖・文化・育成・情報）
- オーナーの動詞のうち **配る・殺す・ぶつける・編む** は P3 以降。
  ただしライバル国の恐怖統治と可読性テストに必要なので `purge()` と `setCanon()` は
  実装してあり、UIには出していない
- 諜報水準による年代記の開示段階（`revealed` フラグは持っているが、投資で開く経路がない）
- 護衛は「随伴した体は他の役に就けない・運死が下がる」まで。護衛が身代わりに死ぬ演出はない
- 派閥の実体（関係値グラフのクラスタ）。個人怨恨の辺は持っているがクラスタ化していない

## 既知の穴

1. **8番染色体だけ腕予算の対象外**（`ARM_EXEMPT`）。設計文書上、8番の対抗は
   「感受性が高い／低い」であって「感受性 vs 他責」ではないため。
   ここに予算を掛けると高感受性×高他責＝扇動者という原型が構造的に作れなくなる。
   代わりに `enforceChromosomeCeiling()` が明示規則で「全座位が親2人を上回る」だけを塞いでいる。

2. **創世の二匹と、根付く前の村（人口6未満のP1）は餓死・事故で死なない。**
   個体数2から始める設計上、ここに平常の死亡率を掛けると1/4近くの世界が
   「第1世代でアダムが狩りの事故で死ぬ」で終わっていた。老衰だけは通している。

3. **性比に弱い自己補正がある**（人口24未満のとき）。小さい村で男ばかり生まれると
   次の世代が存在しなくなるため。人口が増えると効かなくなる。

4. **交配は村内婚（`ENDOGAMY`）だが、兄弟・親子は避ける（`INCEST_AVOID`）。**
   村内婚だけを入れたとき「いちばん近い血をいちばん好む」形になり、実測で
   母親から見た土着の男の平均血縁度が0.90——村がほぼ兄妹婚で回っていた。
   同時に血縁ゼロのよそ者が最も選ばれない相手になり、捕虜が子を残せなかった。
   近親交配の実体は兄妹婚ではなく**いとこ婚・氏族内婚**なので、そちらに寄せてある。
   副産物として雑種強勢の実測値が3.5倍（回復 0.0099 → 0.0349）になった。
   忌避は統治方針では動かない（`inbreedGuard` が効くのはいとこ・氏族の側だけ）。

5. **交配は村内婚（`ENDOGAMY`）である。** 成人男性が50人いる村で能力だけを見て
   相手を選ぶと事実上のランダム交配になり、劣性ホモ率が理論下限 q² から動かない。
   実測で閉鎖世界が 0.136、開放世界の下限が 0.1225——**閉じても腐っていなかった**。
   腐っていないものは治らないので雑種強勢も測れない。血の近さ（親子/兄弟=1.0、
   いとこ=0.5、同じ家=0.35）に応じて結ばれやすくし、`inbreedGuard` だけがそれを打ち消す。
   P1（村）では相手が数人しかいないので効かせていない。

6. **`terror` の粛清→謀反は、時間差の特異性までは実測できていない。**
   発生率の差は明確（0.34件/世代 vs laissez 0.006）で、`traceUp` の鎖も平均3世代前の
   粛清まで届いているが、粛清が毎世代一定量あるので lag別相関が平坦になる。
   「3世代後」を統計で示すには、粛清をパルス状に撃つプロファイルが要る。

   粛清はプロファイルの閾値（`purgeTrigger`）を超えたときにまとめて撃つ形にした。
   日程で毎世代撃っていたときは、粛清と謀反の相関が遅れ0〜6世代で完全に平坦になり、
   定常な相関と区別がつかなかった。オーナーは日程ではなく危険が見えたときに殺す、
   という形のほうが設計にも忠実である。

7. **オーナーごとの分化を成立させているのは選択圧3本の合算**（繁殖機会・粛清・生存）。
   200世代のドリフトは選択圧と同程度の大きさなので、選好は1遺伝子ではなく
   6遺伝子のベクトルにしてある。上げすぎると10国が10通りの極端な単一形質国になるので、
   今の重みは妥協点。

8. **近親交配の強さ（`ENDOGAMY`）は2つの検査の間で綱引きになっている。**
   村内婚を強くすると閉じた血統がよく腐る（近親交配・purist vs melting が通る）が、
   同時に遺伝的荷重が戦闘・繁殖に効いて逃走癖の平衡点が動く（頻度依存が揺れる）。
   3.0 が両者のいちばんましな点だが、どちらの検査も種数が3〜5本しかないため、
   閾値付近では乱数列の引き方で PASS と WARN が入れ替わる。
   **係数を動かして緑にするのはノイズへの過適合**なので、機構として根拠のある値で止めてある。

   なお `takeCaptives` は第2引数が RNG のとき `injectOutsideBlood` に委譲する互換経路を持つ
   （戦争を経ずに外来血だけを入れたいとき用）。本来の
   `takeCaptives(world, battle, axis, rng)` の形は変えていない。

9. **土地（`world.land`）は設計文書にない概念。** 産出の上限を決める資源がないと
   人口が発散するか、逆に「余剰がないと開墾できない」デッドロックで必ず絶滅した。
   生産局面の「規模係数（人数による逓増・逓減）」「土地消耗率／地力回復率」に対応させてある。

11. **出自の選好は「組み合わせ」に効かせている。** 相手選びの重みにだけ掛けていたときは、
   外来の女は候補が全員よそ者なので全候補が同じ係数で割られ、正規化された抽選では
   何も起きなかった——隔離政策なのに外来の血が母系から素通りしていた。
   いまは伴侶を決めてから出生数に掛けるので、父系・母系の両方に効く。

12. **ロスターの10国は同じ創始者から始まる**（`foundSeed` 固定）。
   国ごとに違う初期遺伝子を振っていたときは、創始者2体のくじ運が思想の差を呑み込んで
   クラスタ内分散 > クラスタ間距離になっていた。対照実験として正しい形にしてあるが、
   プレイヤーの世界は性格診断の回答から作るので、この固定は roster.js の中だけ。
