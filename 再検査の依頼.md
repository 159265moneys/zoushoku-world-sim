# 再検査の依頼（増殖）

**宛先**：前回 8班並列で精査してくれたセッション
**HEAD**：`02a4968`　／　**検査 232/232 緑・120年40種で絶滅率 10%（M-10 の目標ちょうど）・平均人口 56.4（正典の実測56）**

前回の指摘をほぼ全部潰しました。再検査をお願いします。

---

## 最優先の2件はどちらもこちらのミスでした

**1｜`world.js:71` の `process.env`**
直しました。しかも帯のコミットで `core/bands.js` に2本目を足していました（typeof ガード＋globalThis 経由に変更）。
**「なぜ 228/228 緑のまま通ったか」のほうが本体**だったので検査を2本足しています：

- 静的：`src` に `process` / `require` / `__dirname` を書かない（typeof ガードとコメントは許す）
- 動的：子プロセスで `globalThis.process` を消してから `src/flow/run.js` を import する

模擬モジュールで実際に赤くなることも確認済み。

**2｜`HIT_DIVISOR` 746 が `Q_DIVISOR` 373→225 に追随していなかった**
450 に直し、定数を `2 * Q_DIVISOR` と書いて二度と離れないようにしました。
**300年7種の平均人口 641 → 1,242、絶滅 3/7 → 1/7。**
保存則 `E[2z/c] = 2p = q` の明文検査と、正典8963 の3点を「分母に追随する形」（2.00 / 1.00 / 0.402 倍）にした検査を追加。旧検査は 746/373/150 を直書きしていたので、分母を変えた瞬間に意味を失っていました。

---

## #17 の欠陥6件

拠点地を潰す／森を食い尽くす／工事人が翌月遊ぶ／分村が職と蔵を捨てる／創世の村の畑27%／`fertYear` の回帰 ── 全部対応。

**ただし2回、自分で壊しました：**

- 「開墾で森林3枚を残す」は**塞ぐ場所が違い**、絶滅率 8% → 38%。正しい塞ぎは `assignWork` が `forestCap` を見て溢れを畑へ回すこと
- 「荒地からも畑を開く」は**地力0の畑**ができて同じく38%（拠点地とまったく同じ罠）

どちらも切り分けて外しました。

結果：120年40種で **絶滅率 5%・平均人口 60.4**（この時点）、300年7種で平均人口 641 → 1,724・絶滅 0/7。

---

## 社会機構

**異端狩り0件の根は「部隊の上限40」**でした（旧 battle.js の「隣の村」時代の数字）。
人口6,000の世界でも兵は40人 → 1人が戦に出る確率2%・生涯0.028回 → 正典3719「戦功3回で rank1」に**構造的に永久に届かない**。

- 割合で決める形に（下限12は残す）＋ `LEVY_SHARE` の焼き込みをやめ「徴兵率」カードを読む
- **逃げた者の討取も事実として残す**（B-33「目に見えてわかる戦果」＝**討取は事実・評判は名誉**。評判 +15 は付けず −20 は付く）

結果：**平民4414/公爵4/騎士1 → 平民5594／騎士14・男爵23・子爵21・伯爵6・侯爵3・公爵23**、局 4 → 9。

**宗教**：誘い手の線 55 → 45。正典6485 は「好き嫌い ≥55」と書き、同じ正典の検算(a) は「信徒の**半分**が誘い手」を前提に 0.00151/月 を出している。実測（同村の大人 25,592組）は **≥55 が19.7%・≥45 が51.7%** ＝ 検算が成り立つのは45側。式は1文字も変えていません（検算の入力で実測 0.00140）。

結果：**信者 0.5% → 平均8.5%（最良の世界で28.5%）・最大の宗派1,022人・改宗 58 → 1,084**。

**異端狩りはまだ0件**ですが、門は正典6047 が「信仰性≥75 かつ 硬さ≥50 … 19.8% … 5宗派に1つ」と直接書いているので**動かしていません**。宗派が 0.8件/世界（正典は0.1〜0.3/村）しか無いのが理由、というのが現時点の結論です。硬さは正典2202「教義の硬さ ← 執念」で発起時に決まる基底の軸なので、狂信では動きません。

---

## そのほか対応済み

- **具申** → 正典4140「農業局が空なら開墾も備蓄も止まる」を接続。局長が居ればカードの割合はその人の**歪み**を通る（柱5が初めて世界に効く）
- **方針カードのUI**（`cards.set()` が検査からしか呼ばれていなかった件）＋「蔵の上限」の二重定義解消。段は±2で止まり、既定に戻すと**0バイト**に戻る
- **捕虜**が男女半々の16〜40歳・寿命あり・`birthTick` も実年齢に（全員が生後0ヶ月の男児だった）
- **消えた宗派の継承**（`inheritMonth` が `alive[]` を見ていなかった）
- **憤怒** 0.0000 → 0.2042（`unmetWrath(0,0,0,0,false)` のハードコード）
- **掟破り3件**：`healMonth` を予備（11番）へ移して無条件抽選／結婚の抽選を候補0でも引く／三つ子の消費を揃えた
- **画面5件**：速度3倍／家 1122/30／蔵が村0番だけ／裏タブ停止（実時間の3/4を毎回捨てていた）／飛ばしボタンが上帯を更新しない
- **星・ハート**を削除（オーナー裁定 2026-08-26）
- **台帳**：M-07・M-08・M-50（平均→中央値のすり替え）・M-53（1種の値／**寿命の床が A-6 の下限40 を割っていた**）・M番号の重複6件

---

## 斥候（#17 §6-8）

「10万人に届かない」の元栓を **372,121回**の分村候補地の判定で分解：
**最小間隔3里 77.8% ／ 霧 15.1% ／ 肥沃 3.7% ／ 居住可能 3.4% ／ 通る 0.02%**

間隔3里は正典11-A、12里の上限は正典7046 で動かせない。**動かせるのは霧だけ**でした ── 正典1740「霧が晴れるのは：**斥候**／行商／分村／戦争」の1本目が実装に無かった。

正典9540 の仕様どおり実装（幅3里の帯・月12里マス・同時最大3人・産出に出ない・月2.5%で死ぬ）。
**既知の里マスが 1,400 → 5,300〜12,400（7〜8倍）**に開きます。

★ 門を入れずに出したら**絶滅率90%**。正典9546「55人から3人抜くと −5.5%。**豊かな村しか探索できない**」がまさにその門で、**働き手18人につき1人**（55/3）を入れて解決。
払える国と払えない国に割れるので**既定はオフ**（軍務局のカード）にしています。

---

## 対応していないもの（正直に）

- **レア度の帯**は組みましたが `BAND=1` の切替えで**既定オフ**。分布はレア度別の平均・中央・全体平均45.4 まで正典の表と一致していますが、106ステのうち**77個（D/C/B/A）の帯が50より下**にあるので、「才能50が中央」で校正された**読み手を全部貼り直す専用の1回**が要ります。寿命・老いの速さ・繁殖力・病への強さ・傷の治り・疲労・必要睡眠 までは貼りましたが、**婚姻の相性・狩りの当たり・戦闘・評判**が残っています
- **PvP／国力／オーナーの動詞5つ／フェーズ＝喪失／因果の台座**は手つかず
- **10万人**も未達（斥候を入れても種で割れる）

---

## 運用

同じ作業ツリーの同時編集は避けたいので、**書き込みはこちらに寄せます**。そちらは写しでの実験を続けてもらえると助かります。
測ってほしい観点があれば、こちらで直します。
